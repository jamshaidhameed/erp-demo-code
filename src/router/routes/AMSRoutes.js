const ROUTE_PREFIX = '/AMS';

const MANAGE_USERS = `${ROUTE_PREFIX}/manage-users`;
const MANAGE_ROLES = `${ROUTE_PREFIX}/manage-roles`;
const USER_TYPES = `${ROUTE_PREFIX}/user-types`;
const DEPARTMENTS = `${ROUTE_PREFIX}/departments`;
const COST_CENTRE = `${ROUTE_PREFIX}/cost-centre`;
const SITES = `${ROUTE_PREFIX}/sites`;

export { ROUTE_PREFIX, MANAGE_USERS, MANAGE_ROLES, USER_TYPES, DEPARTMENTS, COST_CENTRE, SITES };
